specVersion: "v1"
serviceId: "authentication-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "authentication-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
